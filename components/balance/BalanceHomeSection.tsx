import Link from 'next/link';
import { getCurrentMember } from '@/lib/auth';
import { listPublishedArticles } from '@/lib/articles';

export async function BalanceHomeSection(){const [articles,member]=await Promise.all([listPublishedArticles('balance',3),getCurrentMember()]);return <section style={{padding:'34px 0',borderTop:'1px solid var(--border)',marginTop:32}}><div className="homeSectionHead" style={{marginTop:0}}><div><div className="homeKicker">Community editorial</div><h2 className="homeSectionTitle" style={{marginTop:5}}>Balance</h2></div><Link href="/balance" className="btnGhost">Explore Balance →</Link></div>{articles.length?<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(230px,1fr))',gap:16}}>{articles.map(a=><Link href={`/balance/${a.slug}`} key={a.id} style={{border:'1px solid var(--border)',borderRadius:'var(--radius)',overflow:'hidden',background:'var(--bg-raised)'}}>{a.hero_image_url&&<img src={a.hero_image_url} alt={a.hero_image_alt||''} style={{width:'100%',aspectRatio:'16/9',objectFit:'cover'}}/>}<div style={{padding:14}}><div style={{fontSize:11,textTransform:'uppercase',letterSpacing:'.1em',color:'var(--accent-ink)',fontWeight:800}}>{a.article_type.replace('-',' ')}</div><h3 style={{fontSize:20,lineHeight:1.08,margin:'6px 0'}}>{a.title}</h3><div style={{fontSize:12,color:'var(--text-muted)'}}>By {a.author_name} · {a.reading_minutes} min</div></div></Link>)}</div>:<p style={{color:'var(--text-muted)'}}>Member stories are coming to Balance. Be one of the first contributors.</p>}<div style={{marginTop:16}}><Link href={member?'/balance/write':'/login?next=/balance/write'} className="btnAccent">Write for Balance →</Link></div></section>}
